# 디자인 감사 수정계획서

기준: design-review-v2.md (확정 41건) · 작성일: 2026-07-28
원칙: 한 단계 = 한 커밋. 각 단계는 독립 배포 가능하며, 완료 시 해당 감사 항목을 닫는다.

## 0단계 — 이번 커밋에서 이미 반영됨 ✅

Typora 타이포 통일 요청과 겹치는 항목을 함께 처리했다.

| 감사 항목 | 처리 내용 |
|---|---|
| **A1 치명** keep-all 무력화 | `* { word-break: keep-all }`을 테마 뒤에 선언, 코드류만 break-all. 모바일 한글 분절 해소 (스크린샷 재검증 완료) |
| **B4** justify 강(江) | Typora 원본에 justify가 없음 확인 → 본문 좌측 정렬로 회귀. 원인 자체 제거 |
| **B3 부분** 타입 스케일 압축 | Typora 스케일 이식: 본문 h1 1.875em(+헤어라인)/h2·h3 1.3125/h4 1.125, 글 제목 2.2rem으로 승격. h4-본문 역전 해소 |
| **C1 부분** 이탤릭 페이스 부재 | PT Serif 400/700 + 이탤릭 로드, 폰트 스택 1순위로 — 라틴 이탤릭은 진짜 페이스 사용 (한글 자리 이탤릭 정리는 C1 잔여) |
| 사용자 요청 | 본문 16px·행간 1.5em·폭 40em(1400px+에서 914px)·문단 아래 1.5em·인용 5px/2em·코드 0.875em — Typora Newsprint 원본 값 그대로 |

⚠️ 후속 확인 2건: ① 행간 1.5em은 Typora와 동일하지만 한글 장문에는 빡빡할 수 있음 — 라이브에서
읽어보고 답답하면 1.65em 절충. ② 1400px+ 본문 914px는 행이 길어짐(감사 B4 취지와 상충) —
불편하면 미디어쿼리 한 줄 삭제로 40em 고정.

## 1단계 — P0 잔여: 증명 접기 복구 ✅ 완료 (2026-07-28)

- **A2**: `style.css`의 generic `details summary` '⋯'를 이탤릭 'proof' 라벨로 교체 (mathblock+details와 동일 언어).
- **B7**: Black-Litterman 글의 보조정리 2개(`**보조정리 n.**` 문단)를 `{{</* thm */>}}` 숏코드로 이관.
  README에 "정리류는 반드시 숏코드" 규칙 명문화.
- **C13** 겸사: proof summary에 `:focus-visible` 스타일 추가.

## 2단계 — P1: 첫인상 4종 ✅ 완료 (2026-07-28)

- **B1 빈 카드**: post-card.html 분기 — cover 없으면 옅은 적갈 틴트 배경 + 카테고리명 세리프 중앙 배치.
  모바일 그리드 `repeat(2, 1fr)`, 데스크톱 빈 트랙은 카드 최대폭 220px로 해결. featured 운영 원칙(cover 필수) README 추가.
- **B2 검색 축 통일**: 입력창을 결과 열 상단 좌측 정렬로 이동, Esc 닫기 + 포커스 복귀.
- **B5 목차 발견성**: 힌트 선 ink-soft로 상향 + '목차' 라벨 상시 노출, 활성 위치 accent 점.
- **B6 태그 페이지**: `layouts/_default/term.html` 신설 — post-row 재사용, 브레드크럼 톤 통일.

## 3단계 — P2: 접근성 패키지 ✅ 완료 (2026-07-28) — 헤딩 시프트(C17)만 보류

- **B8**: baseof 오버라이드 `<main id="main">` + 스킵 링크, /blog/ 루트 sr-only h1.
- **B2 잔여**: 검색 토글 `aria-expanded`/`aria-controls`, 결과 카운트 `role="status" aria-live="polite"`.
- **C17**: 메뉴·브레드크럼·카테고리 활성 항목에 `aria-current="page"`, nav 2곳 aria-label.
- **C3**: 검색 밑줄 ink-soft + `:focus-visible` 아웃라인 복원. **C18**: 앵커 `ariaLabel`→`aria-label` + 포커스 노출.
- **C17(헤딩)**: 본문 h1 반복 — ⏸ 보류: 헤딩 시프트는 전 글의 시각 위계를 한 단계씩 움직이므로 별도 결정 후 진행.

## 4단계 — P3: 마감 ✅ 완료 (2026-07-28)

- **C2·C4**: 크기 토큰 3종(`--fs-label/-meta/-note`) + 회색 하드코딩 토큰화. README 색 표 갱신.
- **C5** 링크 밑줄 35%→55%. **C6·C7** 푸터 괘선·상단 여백 통일. **C8** 최신 목록에서 featured 제외.
- **C10** 커스텀 404. **C11** 브레이크포인트 700px + 중간 규칙. **C12** 모바일 터치 타깃 패딩.
- **C14** 캡션 중앙정렬 재선언. **C16** 수식 스크롤 힌트 + 증명 종료 ∎. **C19** 검색 입력 1rem.

## 5단계 — 콘텐츠·장기 (수시)

- **C1 잔여**: 한글 섞이는 메타·캡션의 이탤릭 제거(크기·색·자간으로 대체) 여부 결정.
- **C15**: 기존 도판(마젠타·하늘색)을 팔레트 근사색으로 재제작 — 글 작업 시 순차 적용.
- **C9**: Project 빈 상태 문안 한 문장.
- 라이브 전용 검증: KaTeX 렌더 품질(justify 제거 후 수식 밀집 단락), giscus 색, 웹폰트 로딩(PT Serif 추가로 +1패밀리 — 필요 시 Playfair 서브셋화).

## 진행 관리

| 단계 | 상태 | 닫는 항목 |
|---|---|---|
| 0 | ✅ 완료 (이번 커밋) | A1, B4, B3·C1 부분 |
| 1 | ✅ 완료 | A2, B7, C13 |
| 2 | ✅ 완료 | B1, B2(UX), B5, B6 |
| 3 | ✅ 완료 | B8, B2(a11y), C3, aria-current, C18 |
| 4 | ✅ 완료 | C2, C4~C8, C10~C12, C14, C16, C19 |
| 5 | 수시/보류 | C1 잔여, C9, C15, C17 헤딩 시프트(시각 영향 있어 별도 결정) |

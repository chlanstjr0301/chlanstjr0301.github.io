# 디자인 감사 보고서 v2 — Newsprint 개편 이후

감사일: 2026-07-28 · 대상: Martin and Quantitative Investing (Newsprint 개편 완료 시점, 커밋 `b31a260`)
방법: 멀티에이전트 감사 — 7개 차원(타이포그래피/색·대비/레이아웃/정보구조·UX/수학 조판/모바일/접근성) 독립 리뷰
→ 지적별 반박 검증(스크린샷·코드 재확인, 환경 제약 오인·취향 지적 기각). 증거는 전 페이지 × 3뷰포트
스크린샷 16장 + 소스 전체. 리뷰 44건 중 **확정 41건(치명 2 · 구조 15 · 디테일 24), 기각 2건**.
아래는 중복 통합 후 정리본.

**한계**: 샌드박스에서 라이브 스크린샷 불가 → 소스 동일한 로컬 빌드 기준. KaTeX 실제 렌더 품질,
giscus 색 적용, 웹폰트 로딩 성능은 이번 감사 범위 밖 — 라이브에서 별도 확인 필요.

## 총평

Newsprint 전환의 방향성 — 종이/잉크/적갈 단일 포인트, 전면 세리프, 괘선 언어 — 은 성공적으로
안착했고 색 체계 규율도 잘 지켜지고 있다(기각된 2건이 모두 "문제 아님"으로 판명된 것이 그 증거).
문제는 두 곳에 몰려 있다. **첫째, 조판의 실제 적용**: keep-all이 테마 규칙에 덮여 실효되지 않고
(모바일 한글 분절), 타입 스케일이 압축되어 위계가 크기로 읽히지 않는다. **둘째, 관리되지 않은
가장자리**(v1과 같은 패턴): 대표그림 없는 인기글 카드가 빈 액자로 뜨고, 태그·404 페이지가 옛 테마
그대로이며, 넓은 화면의 목차는 존재 자체가 발견되지 않는다. 골격은 완성 — 이제 실효성 검증과
가장자리 청소다.

---

## A. 치명 — 즉시 수정

### A1. 테마의 전역 `word-break: break-word`가 keep-all을 무력화 — 한글이 음절 단위로 분절
`themes/terminal/assets/css/main.css:14-19`의 `*, *::before, *::after { word-break: break-word }`가
모든 요소에 직접 걸려, `static/style.css:29`의 `body { word-break: keep-all }`(상속값)을 덮어쓴다.
390px 본문에서 "논/문의", "사전분/포의", "결/합하여" 같은 단어 중간 줄바꿈이 반복 확인됨
(post-bl-390.png). 신문식 조판이라는 핵심 의도가 **어느 요소에도 적용되지 않고 있는** 전역 결함.
**개선**: style.css에 `.post-content :is(p, li, blockquote, td, figcaption) { word-break: keep-all;
overflow-wrap: break-word; }` 추가(또는 전역 `* { word-break: keep-all }`을 테마 뒤에 선언).
640px 이하에서는 `.post-content p { text-align: left }`로 양쪽정렬 해제 병행.

### A2. Black-Litterman 글의 증명 접기가 라벨 없는 '⋯' — 보조정리 증명을 찾을 수 없음
`style.css:879-893`이 generic `details summary`의 텍스트를 `font-size: 0`으로 지우고 '⋯'만 남기는데,
'proof' 라벨 규칙은 `.mathblock + details`에만 적용된다. BL 글의 보조정리 두 개는 mathblock 숏코드
없이 `**보조정리 1.**` 일반 문단 + `<details>`로 쓰여 있어 인접 셀렉터가 매칭되지 않고, 글 서두에서
예고한 핵심 콘텐츠가 어포던스 없는 회색 점으로만 존재한다(모바일은 hover도 없음).
**개선**: 최소 — generic summary의 '⋯'를 이탤릭 'proof' 라벨로 통일. 근본 — BL 글의 보조정리를
`{{</* thm */>}}` 계열 숏코드로 이관해 연장선 proof UX를 받게 할 것 (B7과 동일 원인).

---

## B. 구조 — 첫인상과 시스템

### B1. 대표그림 없는 인기글 카드 = 빈 액자 (4개 차원에서 독립적으로 적발된 최다 중복 지적)
featured 1순위(Black-Litterman) 카드가 대비 1.12:1의 빈 흰 상자로 렌더. 모바일에서는 1열 풀폭
3:2로 커져 **첫 화면의 절반이 공백**이 된다. 카드 2장 + auto-fill 빈 트랙으로 우측 ~507px 공백도 동반.
**개선**: ① placeholder에 옅은 적갈 틴트 배경 + 카테고리명/이니셜을 Playfair로 중앙 배치(또는 그림
없는 글은 텍스트 카드로 분기), ② 모바일 그리드 `repeat(2, 1fr)` 강제, ③ `auto-fill`→`auto-fit` 또는
카드 최대폭 지정으로 빈 트랙 제거. featured 글에는 cover 지정을 운영 원칙으로.

### B2. 검색 UX의 축 분리 + 스크린리더 침묵
입력창은 우상단(380px), 결과는 좌측 정렬 — 입력과 피드백의 시선 동선이 꺾인다(list-search-open.png).
접근성 측면은 더 심각: 토글에 `aria-expanded` 없음, 결과 영역에 라이브 리전 없음, 기존 목록은 무음으로
hidden 전환 → 비시각 사용자에게 검색이 사실상 동작 불능. Esc 닫기도 없다.
**개선**: 입력창을 결과 열 상단(좌측 정렬)으로 이동해 축 통일. `aria-expanded`/`aria-controls` 토글,
`.search-count`에 `role="status" aria-live="polite"`, Esc 핸들러 + 포커스 복귀. (디테일 C3의 밑줄
대비·포커스 링과 한 패키지로.)

### B3. 타입 스케일 압축 — 위계가 크기로 읽히지 않음
제호 1.75 / 글 제목 1.6 / 본문 h1 1.45(테마값) / h2 1.35 — 층위 간 7~10% 차이라 스캔 시 구분 불가.
h4(1rem)는 본문(1.06rem)보다 작아 **역전**. Playfair는 28px에서 특유의 대비가 뭉개진다.
**개선**: 비율 1.25 스케일 도입 — 제호 2.4~2.6rem, 글 제목 1.9~2.1rem, h1 1.5 / h2 1.25 / h3 1.1.
`.post-content h1~h4` 크기를 테마에 맡기지 말고 직접 선언.

### B4. keep-all + justify + 45자 장행 = 어절 사이 강(江)
A1 수정으로 keep-all이 살아나면 justify가 남는 폭을 어절 사이에 배분해 벌어짐이 생긴다(순수 한글
단락에서도 관찰됨). 라이브에선 인라인 KaTeX가 통짜 박스라 줄바꿈 지점이 더 줄어 악화 가능.
**개선**: (a) 본문 좌측 정렬로 회귀(괘선·단폭으로 신문 질감은 이미 충분), 또는 (b) justify 유지 시
`.post-content p`에 `word-break: normal` + `text-wrap: pretty`. 본문 폭 760→680px(행당 40자)도 검토.
**A1 수정 후 라이브에서 재검증 필수.**

### B5. 1400px+ 목차 사이드바가 발견 불가
내용은 `opacity: 0`, 유일한 힌트는 대비 1.54:1(3:1 미달)의 2px 선 — 스크린샷에서 육안 식별 불가.
넓은 화면에선 접이식 목차도 꺼지므로 긴 수학 글의 핵심 내비게이션이 통째로 숨는다.
**개선**: 힌트 선을 ink-soft(5.2:1)로 + '목차' 라벨 상시 노출, 스크롤 위치를 accent 점으로 표시.

### B6. 태그 페이지가 옛 테마 그대로 — 새 디자인과 완전 단절
글마다 태그 링크가 노출되는데, 클릭하면 영문 "Posts for: #Statistics" + [Read more] 버튼의 테마 기본
레이아웃으로 떨어진다(term.html 오버라이드 부재).
**개선**: `layouts/_default/term.html`을 블로그 목록과 같은 톤(post-row 재사용)으로 신설.

### B7. 정리·증명 시각 시스템이 글마다 다름
숏코드 기반 글(Q-Q, 이자율)은 잉크 선 + 적갈 라벨 + proof 연장선을 받는데, bold 문단 기반 글(BL의
보조정리)은 아무것도 받지 못한다. A2의 원인이자 상위 문제 — 시스템이 콘텐츠 작성 방식에 의존.
**개선**: "정리류는 반드시 숏코드" 작성 규칙을 README에 명문화 + BL 글 이관. (장기: bold 라벨
자동 감지 CSS는 취약하므로 비권장.)

### B8. 접근성 기반 부재 — main 랜드마크·스킵 링크·h1
`<main>` 없음(테마 baseof가 `<div class="content">`), 스킵 링크 없음 → 키보드 사용자는 매 페이지에서
메뉴+카테고리 링크 10여 개를 통과해야 본문 도달. 사실상의 홈인 /blog/ 루트에 h1이 없어 문서가
h2("인기 글")부터 시작. 현재 위치 표시(aria-current)도 전무.
**개선**: baseof 오버라이드로 `<main id="main">` + 스킵 링크, /blog/에 sr-only h1, 활성 메뉴·브레드크럼
끝·활성 카테고리에 `aria-current="page"`, nav 두 곳에 aria-label. 전부 속성 추가 수준의 저비용.

---

## C. 디테일 — 마감 품질 (통합 후 19건)

| # | 지적 | 처방 요지 |
|---|---|---|
| C1 | 이탤릭 페이스 미로드 — 한글·라틴 모두 합성 기울임 (메타·캡션·인용 전부) | 한글 자리는 이탤릭 제거(크기·색·자간으로 부속 톤), 라틴 전용만 이탤릭 유지 |
| C2 | 부속 정보 크기 8종 난립 (0.75~0.95rem) | `--fs-label / --fs-meta / --fs-note` 토큰 3개로 통합 |
| C3 | 검색 밑줄 #b3ab9e 2.03:1 + `outline: none` | ink-soft 밑줄 + `:focus-visible` 아웃라인 복원 |
| C4 | 중립 회색 하드코딩 파편화 (#bababa·#b3ab9e·#e8e7e7) | 토큰화 — 색 규칙 표에 등재 |
| C5 | 본문 링크 밑줄 대비 1.75:1 — 식별이 색 하나에 의존 | 밑줄 혼합비 35%→55% 이상 |
| C6 | 푸터 괘선 폭이 페이지마다 출렁임 (1280 vs 784) | 제호 괘선과 동일한 고정폭 처리 |
| C7 | 제호 괘선 아래 첫 콘텐츠 간격 불일치 (40/75/85px) | 공통 상단 여백 하나로 통일 |
| C8 | 인기 글이 최신 글 목록에 연달아 중복 노출 | 최신 목록에서 featured 제외(한 줄 필터) |
| C9 | Project 빈 페이지 (의도적 유지 확인됨) | 빈 상태 문안만 한 문장으로 다듬기 |
| C10 | 404가 테마 기본 영문 | 커스텀 404 — 제호 톤 + /blog/ 링크 |
| C11 | 768px: 900px 브레이크포인트가 높아 우측 절반이 죽은 공간 | ~700px로 내리고 700–900 구간 `200px 1fr` 중간 규칙 |
| C12 | 모바일 터치 타깃 25–30px (메뉴·카테고리·검색 토글) | ≤900px에서 패딩 확대로 44px 근접 |
| C13 | proof 힌트가 hover 전용 + 포커스 스타일 없음 | `:focus-visible` 추가 (A2와 한 패키지) |
| C14 | 도판 캡션 중앙정렬이 justify에 덮여 좌측 붙음 | figcaption `text-align: center` 재선언 |
| C15 | 도판 내부 색(마젠타·하늘색)이 팔레트 이탈 | 그림 재제작 시 잉크/적갈/틸 근사색 가이드 |
| C16 | 긴 수식 가로 스크롤 단서 없음 · 증명 종료 표식(∎) 없음 | 스크롤 페이드 힌트, `details[open]` 끝에 ∎ |
| C17 | 본문 섹션이 h1 반복 — 제목 h1과 동급 | 렌더 단계 헤딩 시프트 또는 "## 최상위" 규칙 |
| C18 | 헤딩 앵커가 hover 전용 + `ariaLabel` 오타(무효 속성) | `aria-label` 수정, `:focus-visible`에서 노출 |
| C19 | 검색 자동 포커스 시 iOS 확대(입력 16px 미만 아님 — 확인만) | 현행 0.95rem=15.2px, 1rem로 올리면 확대 방지 |

**기각 2건** (검증 단계에서 "문제 아님" 판정): ::selection 청회색(Typora 원본 값 — 의도), 푸터 저자
경로 부재(About에 GitHub·LinkedIn·Email·Resume 버튼 존재 확인).

---

## 우선순위 로드맵

| 순위 | 항목 | 비용 | 효과 |
|---|---|---|---|
| P0 | A1 keep-all 복구 (+640px 좌측 정렬) · A2 증명 라벨 | CSS 몇 줄 + 글 1편 이관 | 모바일 가독성·대표 글 가치 즉시 회복 |
| P1 | B1 빈 카드 · B2 검색 축+SR · B5 목차 발견성 · B6 태그 페이지 | 반나절 | 첫인상·핵심 동선 완성 |
| P2 | B3 타입 스케일 · B4 justify 정책(라이브 재검증) · B7 숏코드 규칙 | 반나절 | 조판 시스템 확립 |
| P3 | B8 + C3·C13·C17·C18 접근성 패키지 | 2~3시간 | WCAG 기반 확보, 대부분 속성 추가 |
| P4 | C 나머지 마감 (토큰화·404·768px·터치 타깃 등) | 반나절 | 장기 유지보수·완성도 |

한 문장 요약: **Newsprint의 골격과 색 규율은 이미 완성 — 남은 것은 "선언한 조판이 실제로
적용되는지"의 검증(A1·B4)과, v1 때와 같은 가장자리 청소(빈 카드·태그·404·목차)다.**

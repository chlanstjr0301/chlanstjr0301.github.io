# Munseok's Blog 종합 평가 보고서

평가일: 2026-07-08 · 대상: https://chlanstjr0301.github.io/ (라이브) + 로컬 소스 전체
관점: "깔끔함"을 기준으로 한 수학·디자인 종합 리뷰. 핵심은 문제와 개선방안.

## 총평

방향성은 이미 좋다. 다크 터미널 골격 위에 세리프 본문, 절제된 3색 체계(오렌지=구조, 샌드=링크, 틸=수학), 숨김형 목차, 선 하나로 통일된 정리 환경 — "필요할 때만 나타나는 인터페이스"라는 일관된 철학이 있다. 문제는 디테일이 아니라 **관리되지 않은 가장자리**에 있다: 빈 글이 배포되어 있고, 홈과 트리가 "완성된 글 2편"이 아니라 "빈 계획표 24칸"을 먼저 보여준다. 방문자가 처음 만나는 화면일수록 관리 상태가 나쁘다.

---

## A. 치명 — 즉시 수정

### A1. 빈 글 2개가 라이브에 배포됨
`Kalman-Filter-EKF.md`, `Kalman-Filter-UKF.md`가 **내용 0바이트인 채 커밋·배포**되어 있다. front matter가 없으니 draft 보호도 못 받아, 홈 목록에 제목 없는 카드 2개 + RSS에도 빈 항목으로 노출 중. 블로그 신뢰도를 가장 크게 깎는 요소.
**개선**: 두 파일에 `draft: true` front matter를 넣거나 발행 전까지 커밋에서 제외. 추가 안전장치로 예제 템플릿을 복사해 시작하는 습관 고정.

### A2. KF 초안의 시한폭탄
`Kalman-Filter-KF.md`는 draft라 안 새고 있지만, 예제 템플릿을 그대로 복사해 **title이 "Q-Q Plot"**이다. 이대로 draft만 풀면 Q-Q Plot 글이 두 개가 된다.
**개선**: 지금 제목·태그·description을 실제 값으로 교체해 둘 것.

### A3. 날짜 불일치
CRR 글: 파일명은 `2026-05-26`, front matter는 `2026-07-04`. 표시되는 건 front matter 쪽이라 파일명과 어긋난다. 아카이브가 쌓일수록 정렬·URL·기억이 어긋나는 원인.
**개선**: 규칙을 하나로 — "파일명 날짜 = 최초 작성일 = front matter date". 수정일은 `lastmod`로 분리.

---

## B. 정보 구조 — 첫인상을 결정하는 곳

### B1. 홈이 역할 없이 소음만 있음
현재 홈 = 최근 글 풀카드 5개(제목+날짜+태그 최대 6개+설명+Read more). 글이 2편뿐인 지금, 빈 칼만 카드까지 섞여 "정신없다"는 본인 평가가 정확하다. 태그 6개 나열이 카드에서 가장 큰 시각적 소음.
**개선**: 홈을 명함으로. 한 줄 소개(Be guided by beauty) + 최근 글 3개를 "제목 · 날짜" 한 줄씩만. 태그는 글 페이지 안에서만. 대안: 홈에서 `/blog/` 트리로 바로 보내고 홈을 없애는 것 — 더 과감하고 더 깔끔하다.

### B2. 트리가 "서재"가 아니라 "빈 계획표"
[Articles] 트리의 카테고리 약 24개 중 글이 있는 곳은 2곳. 방문자에게 빈 서랍 22개를 먼저 보여주는 구조다. 본인용 로드맵으로는 가치가 있지만 공개 첫 화면으로는 미완성 인상을 준다.
**개선**: 글 없는 카테고리는 렌더링에서 숨기기(글이 생기면 자동 등장 — 지금 구조상 템플릿 한 줄 조건이면 됨). 로드맵을 남기고 싶다면 About에 "다룰 주제" 문단으로 옮기는 편이 정직하다.

### B3. 새 글을 발견할 경로가 없음
트리는 글 제목을 다 숨기고, 날짜도 없다. 재방문자가 "뭐가 새로 올라왔지?"를 알 방법이 홈 카드뿐인데 그 홈도 정리 대상.
**개선**: 트리 상단에 "Recently" 2–3줄(제목·날짜만) 또는 홈이 그 역할을 확실히 맡기. 둘 중 하나면 충분, 둘 다는 과잉.

### B4. 자잘한 구조 문제
- "Relative Mathematics"가 Financial Engineering과 Economics 양쪽에 존재 — 의도라면 유지, 아니면 명칭 분화.
- Project 페이지가 "추후 추가" 한 줄인 채 메뉴에 노출 — 채우기 전까지 메뉴에서 빼는 게 깔끔함의 원칙에 맞다.

---

## C. 타이포그래피·색

### C1. 모노 한글의 가독성
메타·트리·메뉴의 Nanum Gothic Coding은 터미널 정체성에 맞지만 자간이 넓어 한글 문장에는 부적합. 지금은 짧은 라벨에만 쓰여 괜찮으나, 홈 카드의 description까지 모노로 흐르면 답답해진다.
**개선**: 규칙 명문화 — "문장은 세리프, 라벨은 모노". description·트리 안내문은 세리프로.

### C2. 저대비 요소가 기준 미달
proof 힌트(40%), 트리 Expand 버튼(50%), 각주 백링크(60%) 등이 WCAG AA(4.5:1)에 못 미친다. "흐림=미학"이지만 흐림에도 바닥이 필요하다.
**개선**: 비활성·힌트류 최소 55–60% 불투명도로 상향. 호버 시 100%는 현행 유지.

### C3. 3색 체계 — 좋으나 문서화가 없음
오렌지(구조·인터랙션)/샌드(링크·태그)/틸(수학)의 역할 분담은 이 블로그의 자산. 다만 규칙이 코드 주석에만 흩어져 있어 미래의 본인이 어길 확률이 높다.
**개선**: README에 색 역할 표 5줄 추가. 새 요소를 만들 때 "이건 구조인가, 링크인가, 수학인가"만 물으면 된다.

---

## D. 글 페이지 — 수학 조판

### D1. 정리 라벨의 위계가 약함
블록을 선 하나로 통일한 것은 옳았다. 그 결과 이제 구분을 라벨 텍스트(볼드)가 홀로 지는데, 본문 볼드와 구별이 안 돼 스캔할 때 Theorem과 강조 문장이 섞인다.
**개선**: 쇼트코드에서 라벨 부분만 틸색 또는 small-caps 처리. 선은 그대로 하나.

### D2. 초장문 증명의 스크롤 압박
Theorem 1(Galois 부등식) 증명이 500줄 이상. 펼치는 순간 글의 리듬이 무너진다.
**개선**: 증명 내부에 단계 번호((i), (ii), …)와 중간 소결론을 넣거나, 이 정도 분량은 "증명 노트" 별도 글로 분리하고 본문엔 스케치만.

### D3. KaTeX 로딩 순간 원문 노출(FOUC)
CDN defer 렌더라 수식 많은 글에서 `$...$` 원문이 잠깐 보인다. 수학 블로그에서 눈에 띄는 결점.
**개선**: 단기 — `math:true`인 본문을 렌더 완료까지 살짝 숨기는 CSS 한 줄(opacity 전환). 장기 — 서버측 KaTeX 렌더링.

### D4. KaTeX가 모든 페이지에 로드됨
`hugo.toml`의 `params.math = true`가 사이트 전역이라 About·Project·트리에서도 KaTeX JS/CSS를 내려받는다.
**개선**: 전역 기본값을 끄고 글별 front matter `math: true`로만 켜기. 수식 없는 페이지 첫 로드가 눈에 띄게 가벼워진다.

---

## E. 기술·성능·SEO

### E1. OG 이미지·파비콘이 테마 기본값
공유 카드 이미지(`og-image.png`)와 파비콘이 panr 테마 기본 그대로다. 링크를 공유하면 남의 테마 로고가 뜬다.
**개선**: `static/og-image.png`(1200×627)와 `static/favicon.png`만 만들어 넣으면 테마 것을 덮어쓴다. 다크 배경 + "M" 또는 수식 한 줄이면 충분.

### E2. 웹폰트 3패밀리 로드
Noto Serif KR + Noto Sans KR + Nanum Gothic Coding(+ Fira Code는 테마). Sans KR은 About 한 페이지용인데 전역 로드 중.
**개선**: Sans KR 웨이트를 400·700만 남기거나, About의 sans를 시스템 폰트 스택으로 대체해 한 패밀리 절약.

### E3. style.css 772줄 — 카스케이드 부채
반복 개편의 역사가 "나중 규칙이 앞 규칙을 덮는" 구조로 쌓여 있다(같은 셀렉터 재정의 다수). 지금은 동작하지만 반년 뒤 수정이 러시안룰렛이 된다.
**개선**: 기능별 재편성 1회(변수 → 레이아웃 → 트리 → 글 → 수학 → 유틸), 죽은 규칙 삭제. 반나절 투자로 유지보수성이 크게 오른다.

---

## 우선순위 로드맵

| 순위 | 항목 | 비용 | 효과 |
|---|---|---|---|
| P0 | A1·A2 빈 글/시한폭탄 draft 처리 | 5분 | 신뢰도 즉시 회복 |
| P1 | B1 홈 명함화 + B2 빈 카테고리 숨김 | 1–2시간 | 첫인상 완전히 달라짐 |
| P2 | E1 OG·파비콘, A3 날짜 규칙 | 30분 | 공유·아카이브 품질 |
| P3 | D1 정리 라벨, C2 대비 상향, D4 KaTeX 조건부 로드 | 1시간 | 조판·성능 마감 |
| P4 | E3 CSS 재편성, D3 FOUC, E2 폰트 다이어트 | 반나절 | 장기 유지보수 |

한 문장 요약: **뼈대와 취향은 이미 상위권 — 지금 필요한 건 새 기능이 아니라 가장자리 청소다.**
